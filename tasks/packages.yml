- name: Install Standard Packages
  package:
    name:
      - htop
      - neovim
      - tmux
      - ncdu
      - git
      - curl
      - openssh-server
      - netdata
  ignore_errors: yes

- name: Disable netdata
  systemd:
    name: netdata
    enabled: yes
    state: started
  ignore_errors: yes


# - name: Set unattended updates
