- name: Install Standard Packages
  package:
    name:
      - htop
      - neovim
      - tmux
      - ncdu
      - git
      - curl
      - openssh-server
  ignore_errors: yes

- name: Install netdata
  package:
    name: netdata
    state: present
  notify: Disable netdata


# - name: Set unattended updates
