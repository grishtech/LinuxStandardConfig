- name: Set security
  copy:
    src: files/sshd_config
    dest: /etc/ssh/sshd_config
    owner: root
    group: root
    mode: 644

- name: restart ssh service
  systemd:
    name: sshd
    state: restarted
    daemon_reload: yes